---
title: Articles and Notes
---

#all-posts
  - categories.each do |category|
    .category
      %h1= category.capitalize
      - tags( category ).each do |tag|
        %h2= tag
        - tagged_as( tag ).each do |post|
          = partial 'layouts/partials/card', :locals => {:post => post}
